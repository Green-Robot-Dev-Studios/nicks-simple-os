#include "../stdlib/stddef.h"

// byte port_byte_in(unsigned short port) {
//     byte result;
//     __asm__("in %%dx, %%al" : "=a" (result) : "d" (port));
// }

// byte port_byte_out(unsigned short port, byte data) {
//     unsigned short result;
//     __asm__("out %%dx, %%ax" : "=a" (result) : "d" (port));
// }
